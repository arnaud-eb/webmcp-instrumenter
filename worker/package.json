{
  "name": "webmcp-logger",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker + D1 logging sink for WebMCP invocation events (insert-only, param_keys only).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
